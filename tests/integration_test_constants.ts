// Delay bgetween API calls to ensure we don't slam our own/ip-api.com's APIs
export const delay_ms = 500;

// Just the lat/long fields for testing filtered down results
export const just_lat_lng = [ 'lat', 'lon' ];
